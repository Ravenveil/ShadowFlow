# LEGACY / HISTORICAL NOTE

> 这是一份历史文档，已停止作为当前主线实施依据。
> 请不要将其中的设计建议、示例代码、路线图或分层架构，视为现行规范。

## 当前主线入口

当前项目的主线定义与实现依据，请以以下文档为准：

- `CORE_CHARTER`
- `RUNTIME_CONTRACT_SPEC`
- `WORKFLOW_SCHEMA`
- `ADAPTER_BOUNDARY`
- `docs/api/http/README.md`

## 这份文档保留什么

仅保留一段历史背景，便于理解早期探索方向：

- 这里曾讨论过 AgentGraph 与 LangGraph 的关系。
- 这里曾尝试描述多智能体编排、共享记忆、路由与协议层的组合思路。
- 这些内容属于旧方案记录，不代表当前设计、接口、边界或实现顺序。

## 不再保留什么

以下内容已从主线意义上失效，阅读时请直接忽略其实施性：

- 具体分层架构与协议栈拆分
- 代码骨架、类定义、伪代码和 YAML 示例
- 路线图、里程碑、工期预估与“立即可执行”清单
- 任何看起来像产品设计说明、开发指南或落地方案的段落

## 使用约定

如果你需要当前版本的真实约束、接口和边界，请直接跳到上面的主线文档。
这份文件只用于历史追溯和上下文理解，不参与当前实现决策。
