# 2026-03-23 AgentGraph Brief

## 今日一句话总结

把剩余 AgentGraph 历史大文档与 `tests/legacy/` 目录化一起收口，默认主线入口和默认测试入口现在都更明确地站在 Phase 1 runtime contract 一侧。

## 上一轮进展承接

- 延续上一轮什么：
  - 延续 `legacy surface 收敛` 主战役，重点继续处理剩余历史大文档与 legacy tests。
- 完成上一轮哪部分：
  - 把“剩余历史大文档 + `tests/legacy/` 目录化”真正做成结构化收口，而不是停留在 marker 和 note。
- 放弃上一轮哪部分：
  - 没有继续扩 runtime 新能力。
  - 没有把 Shadow 背景稿当作本轮主成果主体。
- 为什么：
  - 当前高杠杆动作是把 AgentGraph 自己的历史资产和默认测试入口收干净，进一步降低主线推进噪音。

## 本轮规划摘要

- 唯一主大任务：
  - 剩余 AgentGraph 历史大文档 + `tests/legacy/` 目录化收口
- work package：
  - 多 worker 并行收剩余历史文档
  - 本地完成 legacy tests 目录迁移
  - docs 索引与 legacy map 收口
  - 回归验证与状态写回

## 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：彻底完成剩余 AgentGraph 历史大文档与 legacy tests 目录化收口

## 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 10/10 个关键子任务

## 执行结果

- 多智能体并行收口剩余历史文档：
  - `docs/agentgraph与langgraph`
  - `docs/agentgraph计划书`
  - `docs/shadow_integration_strategy.md`
  - `docs/SHADOW_CLAW_DESIGN.md`
  - `docs/shadow_system_master_strategy.md`
  - `docs/shadow_system_unified_strategy.md`
  - `docs/unified_strategy.md`
- 完成 legacy tests 目录化：
  - 新增 `tests/legacy/`
  - 历史测试从 `tests/` 根目录迁移到 `tests/legacy/`
  - 新增 `tests/legacy/README.md`
  - 新增 `tests/legacy/__init__.py`
- 收口测试入口：
  - `tests/conftest.py` 改为按目录识别 legacy tests
  - `tests/README.md` 更新为目录化口径
- 收口文档入口：
  - `docs/README.md` 区分了：
    - 当前主线文档
    - AgentGraph 历史大文档
    - 仓内跨项目背景稿
    - 测试口径入口
  - `docs/LEGACY_SURFACE_MAP.md` 新增 `tests/legacy/` 和剩余历史文档分类

## 成熟度推进

- AgentGraph 从“主线 contract 已清楚，但历史文档和 legacy tests 仍散落”推进到“历史大文档和历史测试都被显式归位”的阶段。
- 这不是新能力扩张，但显著降低了主线继续被历史入口和历史验证口径打断的概率。

## docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实“先收敛 runtime，再扩张叙事”
- `LEGACY_SURFACE_MAP`
  - 已从文档/示例边界继续扩展到测试目录边界
- `WHAT_IS_AGENTGRAPH`
  - 当前主线入口与历史叙事入口更清晰分离
- `agentgraph-phase1-campaign-draft`
  - 更接近“默认入口与默认验证口径都围绕当前 contract 主线”的状态

## 代码审查摘要

- 本轮只审查本轮改动文件：
  - `docs/README.md`
  - `docs/LEGACY_SURFACE_MAP.md`
  - `docs/agentgraph与langgraph`
  - `docs/agentgraph计划书`
  - `docs/shadow_integration_strategy.md`
  - `docs/SHADOW_CLAW_DESIGN.md`
  - `docs/shadow_system_master_strategy.md`
  - `docs/shadow_system_unified_strategy.md`
  - `docs/unified_strategy.md`
  - `tests/conftest.py`
  - `tests/README.md`
  - `tests/legacy/*`
- 重点结论：
  - 默认测试入口已不再依赖文件名单维护 legacy 测试
  - AgentGraph 历史文档与仓内跨项目背景稿已分开表述
  - 本轮没有破坏 runtime contract、CLI/HTTP 路径或官方样例验证链

## 验证结果

- `pytest -q`
  - 7 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\parallel-synthesis.yaml`
  - valid=true

## 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1-2 周。
- 依据：
  - 主线能力、主线样例、默认入口、默认测试口径都已较稳定
  - 剩余更集中在 legacy 示例迁移、adapter 消费边界细化和更完整验证矩阵

## 下一轮建议

- 优先继续处理剩余 legacy 示例迁移
- 再推进 adapter 消费边界与官方验证矩阵扩展
- 如需继续降噪，可进一步压缩 legacy Python API 对外叙事

---

## 续推进补记

### 今日一句话补充

这轮不再只是文档降噪，而是把 legacy 示例迁移正式落成了代码里的官方样例注册表与参数化验证矩阵。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 `legacy surface 收敛` 主战役，但把重点从历史文档/测试收口切到 legacy 示例迁移。
- 完成上一轮哪部分：
  - 真正完成了“legacy 示例迁移与官方验证矩阵扩展”。
- 放弃上一轮哪部分：
  - 没有继续把本轮主成果放在文档治理。
- 为什么：
- 当前更高杠杆的推进，是把示例从“说明材料”升级成“代码中的官方资产与自动化验证基线”。

### 本轮规划摘要

- 唯一主大任务：
  - legacy 示例迁移与官方验证矩阵扩展
- work package：
  - 多 worker 并行迁移高价值 legacy 示例
  - 本地实现官方样例注册表
  - 扩展参数化测试矩阵
  - 回归验证与状态写回

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：把 legacy 示例迁移正式收敛成代码里的官方样例资产与验证基线

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 11/11 个关键子任务

### 执行结果

- 新增代码：
  - `agentgraph/runtime/official_examples.py`
    - 提供官方样例 manifest 读取、样例枚举、统一 workflow 加载
- 新增官方样例注册表：
  - `examples/runtime-contract/official-examples.yaml`
- 新增迁移后的 canonical 样例：
  - `examples/runtime-contract/simple-assistant.yaml`
  - `examples/runtime-contract/code-review-phase1.yaml`
  - `examples/runtime-contract/research-report-phase1.yaml`
  - `examples/runtime-contract/content-creation-phase1.yaml`
  - `examples/runtime-contract/data-processing-phase1.yaml`
  - `examples/runtime-contract/github-monitoring-phase1.yaml`
- 更新：
  - `agentgraph/runtime/__init__.py`
    - 暴露官方样例注册表相关 API
  - `tests/test_runtime_examples.py`
    - 改成围绕 `official-examples.yaml` 的参数化验证矩阵
    - 覆盖 manifest 一致性、service validate/run、CLI validate
  - `examples/README.md`
  - `docs/WORKFLOW_SCHEMA.md`
    - 补充官方样例注册表入口与新增 canonical 示例

### 成熟度推进

- AgentGraph 从“只有少量手工维护的官方示例”推进到“官方样例有代码注册表、有统一加载接口、有自动化验证矩阵”的阶段。
- 这意味着示例不再只是文档材料，而是开始成为 runtime contract 的正式回归资产。

### docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实“先收敛 runtime，再扩张叙事”，因为这轮主推进点在代码和测试基线
- `WORKFLOW_SCHEMA`
  - 已把新增 canonical 示例和官方样例注册表纳入主线说明
- `RUNTIME_CONTRACT_SPEC`
  - 通过参数化验证矩阵，更接近“由代码与测试共同维护契约”

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `agentgraph/runtime/official_examples.py`
  - `agentgraph/runtime/__init__.py`
  - `examples/runtime-contract/official-examples.yaml`
  - 6 个新增 canonical 示例
  - `tests/test_runtime_examples.py`
  - `examples/README.md`
  - `docs/WORKFLOW_SCHEMA.md`
- 重点结论：
  - 官方样例不再手工散列在测试里，而是统一由 manifest 驱动
  - 迁移后的 YAML 都对齐当前 `WorkflowDefinition`
  - 当前验证矩阵已经覆盖 service 与 CLI 两条主链

### 验证结果

- `pytest -q`
  - 25 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\github-monitoring-phase1.yaml`
  - valid=true
- `python -m agentgraph.cli run -w examples\runtime-contract\content-creation-phase1.yaml -i '{"goal":"Create a publishable content draft"}'`
  - succeeded

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1-2 周。
- 依据：
  - 现在官方样例已经开始被代码化、注册化、矩阵化
  - 剩余更集中在 adapter boundary、checkpoint/resume 官方基线与未迁移 legacy 示例

### 下一轮建议

- 优先把 checkpoint/resume 也纳入官方样例验证矩阵
- 再推进 adapter boundary 的宿主消费约定与测试
- 如需继续迁移 legacy 内容，优先选择仍有高集成价值的旧模板和示例

---

## 续推进补记 2

### 今日一句话补充

checkpoint/resume 与 adapter boundary 已经进入默认自动化基线，不再只是文档承诺。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续官方样例注册表和参数化验证矩阵。
- 完成上一轮哪部分：
  - 把 resume 和 adapter boundary 正式纳入了官方样例矩阵与 HTTP/service 测试。
- 放弃上一轮哪部分：
  - 没有继续新增更多 legacy 示例迁移。
- 为什么：
  - 当前主线更需要验证恢复链和宿主消费边界，而不是继续扩示例数量。

### 本轮规划摘要

- 唯一主大任务：
  - checkpoint/resume 官方样例矩阵 + adapter boundary 验证基线
- work package：
  - 扩展官方样例 manifest 的 resume 元数据
  - 扩展 resume 参数化测试
  - 扩展 HTTP adapter boundary 基线
  - 回归验证与状态写回

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：把 checkpoint/resume 和 adapter boundary 拉进默认主线验证矩阵

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 9/9 个关键子任务

### 执行结果

- 更新：
  - `agentgraph/runtime/official_examples.py`
    - 支持 resume 相关元数据字段
  - `examples/runtime-contract/official-examples.yaml`
    - 为 `research-review-loop`、`parallel-synthesis`、`content-creation-phase1` 增加 resume 期望
  - `tests/test_runtime_examples.py`
    - 新增基于 manifest 的 `run -> checkpoint -> resume` 参数化测试
  - `tests/test_runtime_contract.py`
    - 新增 HTTP adapter boundary 基线测试
    - 新增 parallel `branch_outputs` boundary 断言
  - `docs/ADAPTER_BOUNDARY.md`
    - 补充官方验证基线说明

### 成熟度推进

- AgentGraph 从“官方样例矩阵只覆盖 validate/run”推进到“官方样例矩阵已经开始覆盖 resume 与 adapter boundary”的阶段。
- 这一步更贴近“独立、稳定、可集成”的标准，因为它直接验证外部宿主如何调用和恢复。

### docs 思想落实进度

- `ADAPTER_BOUNDARY`
  - 已从抽象说明推进到“有对应自动化验证基线”
- `RUNTIME_CONTRACT_SPEC`
  - checkpoint/resume 的 contract 现在更接近由代码与测试共同守护
- `CORE_CHARTER`
  - 继续落实“先收敛 runtime 主线，再谈外围能力”

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `agentgraph/runtime/official_examples.py`
  - `examples/runtime-contract/official-examples.yaml`
  - `tests/test_runtime_examples.py`
  - `tests/test_runtime_contract.py`
  - `docs/ADAPTER_BOUNDARY.md`
- 重点结论：
  - resume 测试已经不依赖手工选 checkpoint，而是由官方样例 manifest 驱动
  - HTTP adapter boundary 的最小宿主链已经形成自动化断言
  - parallel 输出中的 `branch_outputs` 也进入了 boundary baseline

### 验证结果

- `pytest -q`
  - 30 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\parallel-synthesis.yaml`
  - valid=true
- `python -m agentgraph.cli run -w examples\runtime-contract\research-review-loop.yaml -i '{"goal":"Produce a research review"}'`
  - succeeded

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1-2 周。
- 依据：
  - 官方样例、resume 链和 adapter boundary 都开始纳入统一基线
  - 剩余更多是边界细化，而不是主干缺失

### 下一轮建议

- 优先细化 artifact/checkpoint/writeback 的宿主消费约定
- 再评估 checkpoint store / multi-tenant 的 Phase 1.5 边界
- 如需继续扩矩阵，可加入更多 resume 场景和失败路径

---

## 续推进补记 3

### 今日一句话补充

artifact/checkpoint/writeback 已经从“宿主自己理解”推进到“runtime 明确返回 + 默认测试严格校验”的阶段。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 checkpoint/resume 官方样例矩阵与 adapter boundary 基线。
- 完成上一轮哪部分：
  - 把 artifact/checkpoint/writeback 宿主消费约定真正落到契约模型、运行时输出和测试里。
- 放弃上一轮哪部分：
  - 没有继续扩更多官方样例或新能力类型。
- 为什么：
  - 当前最关键的是把宿主 writeback 真正依赖的字段固定下来，而不是继续扩大表面积。

### 本轮规划摘要

- 唯一主大任务：
  - artifact/checkpoint/writeback adapter 契约 + 对应验证基线
- work package：
  - 收紧 runtime contract 模型
  - 在 runtime service 生成标准 writeback envelope
  - 扩展测试到字段级一致性断言
  - 补专门的 writeback adapter contract 文档并写回状态

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：把 artifact/checkpoint/writeback 宿主消费约定收紧为可执行契约

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 10/10 个关键子任务

### 执行结果

- 更新：
  - `agentgraph/runtime/contracts.py`
    - 新增 `WritebackRef`
    - `ArtifactRef` / `CheckpointRef` 增加显式 `writeback`
  - `agentgraph/runtime/service.py`
    - artifact/checkpoint 统一生成 writeback envelope
  - `agentgraph/runtime/__init__.py`
    - 暴露 `WritebackRef`
  - `tests/test_runtime_examples.py`
    - 收紧 writeback 字段级一致性断言
  - `tests/test_runtime_contract.py`
    - 收紧 HTTP / service 的 artifact/checkpoint writeback 断言
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/README.md`
- 新增：
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`

### 成熟度推进

- AgentGraph 从“checkpoint/resume 和 adapter boundary 已有基线”推进到“artifact/checkpoint/writeback 契约已经是显式字段和严格测试对象”的阶段。
- 这一步让宿主集成边界更像真正可消费的 adapter contract，而不是口头约定。

### docs 思想落实进度

- `ADAPTER_BOUNDARY`
  - 已进一步从对象级边界推进到 writeback 字段级边界
- `RUNTIME_CONTRACT_SPEC`
  - 已补充 artifact/checkpoint 的 writeback 要求
- `WRITEBACK_ADAPTER_CONTRACT`
  - 新增并固定宿主最小消费约定

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `agentgraph/runtime/contracts.py`
  - `agentgraph/runtime/service.py`
  - `agentgraph/runtime/__init__.py`
  - `tests/test_runtime_examples.py`
  - `tests/test_runtime_contract.py`
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/README.md`
- 重点结论：
  - writeback 不再埋在 metadata 约定里，而是进入显式 contract 字段
  - runtime 输出和 HTTP 响应的 writeback 结构已经自动化覆盖
  - 当前收紧是增量兼容的，没有打断既有 run/result 主链

### 验证结果

- `pytest -q`
  - 30 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\content-creation-phase1.yaml`
  - valid=true
- `python -m agentgraph.cli run -w examples\runtime-contract\parallel-synthesis.yaml -i '{"goal":"Synthesize two research branches"}'`
  - succeeded

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1-2 周。
- 依据：
  - 主线 contract、官方样例、resume、adapter boundary、writeback 契约都已进入默认测试基线
  - 剩余更多是进一步细化宿主场景，而不是补主干缺口

### 下一轮建议

- 优先细化 `writeback.target` / `writeback.mode` 的宿主场景边界
- 再评估 checkpoint store / multi-tenant 的最小外部契约
- 如需继续加测试，优先覆盖 writeback 失败路径和宿主侧重试语义

---

## 续推进补记 4

### 今日一句话补充

`writeback.target / writeback.mode` 已从静态字段推进为可配置宿主契约，失败路径和核心 docs 也已同步进入默认主线基线。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 artifact/checkpoint/writeback 契约、官方样例矩阵、resume 和 adapter boundary 主线。
- 完成上一轮哪部分：
  - 完成了 `writeback.target / writeback.mode` 宿主场景化、优先级和失败路径的代码实现与验证。
- 放弃上一轮哪部分：
  - 没有进入 checkpoint store / multi-tenant
  - 没有开始真实宿主 writeback adapter 实现
- 为什么：
  - 当前高杠杆动作是先把宿主真正会消费的 writeback 契约、失败语义和权威 docs 完全对齐。

### 本轮规划摘要

- 唯一主大任务：
  - `writeback.target / writeback.mode` 宿主场景化 + writeback 失败路径验证基线
- work package：
  - contract/service 优先级与失败路径实现
  - 官方样例场景化
  - service/HTTP/CLI 验证
  - 核心 docs 与状态文件同步

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：把 writeback 从“固定 host/reference”推进到宿主可依赖的场景化契约

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 12/12 个关键子任务

### 执行结果

- 已完成的核心代码成果已在主线提交：
  - `3054b34 feat: scenarioize writeback targets and failures`
- 这轮继续补齐并同步：
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/WORKFLOW_SCHEMA.md`
  - `docs/status/session-memory.md`
  - `docs/status/daily-briefs/2026-03-23-brief.md`
- 当前 writeback 已支持：
  - workflow defaults
  - request metadata override
  - node-level artifact override
  - invalid target/mode 校验失败
  - inline 无内容运行失败

### 成熟度推进

- AgentGraph 从“有 writeback 字段”推进到“writeback 有 target/mode 场景、有优先级、有失败语义、有默认验证基线”的阶段。
- 当前已经能作为受控 Phase 1 编排 runtime 被宿主调用，而不是只停留在样例展示层。

### docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实 contract-first 与 runtime-first
- `RUNTIME_CONTRACT_SPEC`
  - 已同步 writeback 场景化、RunResult 当前结构和错误分类补充
- `WORKFLOW_SCHEMA`
  - 已同步 `defaults.writeback`、节点级 artifact override 与合法组合约束
- `WRITEBACK_ADAPTER_CONTRACT`
  - 已从“固定 host/reference”改成场景化宿主消费契约
- `ADAPTER_BOUNDARY`
  - 已补 writeback 分流规则与失败路径基线说明

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/WORKFLOW_SCHEMA.md`
  - `docs/status/session-memory.md`
  - `docs/status/daily-briefs/2026-03-23-brief.md`
- 重点结论：
  - docs 已不再落后于主线代码
  - writeback 场景化语义、失败路径和宿主消费规则已在权威文档层收口
  - 当前剩余风险不在 contract，而在真实宿主 adapter 还没落地

### 验证结果

- `pytest -q`
  - 38 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\content-creation-phase1.yaml`
  - valid=true
- `python -m agentgraph.cli run -w examples\runtime-contract\docs-gap-review.yaml -i '{"goal":"Analyze docs coverage and produce review notes"}'`
  - succeeded
- `git diff --check`
  - 通过

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1-2 周。
- 依据：
  - contract、CLI/HTTP、checkpoint/resume、官方样例、adapter boundary、writeback 场景化和失败路径都已进入默认基线
  - 剩余更集中在真实宿主 adapter 和 checkpoint store 边界

### 下一轮建议

- 优先实现最小 `writeback adapter reference implementation`
- 再补 `checkpoint store` 最小外部契约
- 然后再考虑 writeback 失败后的最小重试语义

---

## 续推进补记 5

### 今日一句话补充

最小宿主桥已经搭出来了：AgentGraph 现在有 `docs / memory / graph` 三路 reference adapter stub，也有可共享的 in-memory checkpoint store 最小契约。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 writeback 场景化、adapter boundary、checkpoint/resume 主线。
- 完成上一轮哪部分：
  - 把“writeback adapter reference implementation + checkpoint store 最小契约”真正落到代码与测试里。
- 放弃上一轮哪部分：
  - 没有直接做 Shadow 私有 adapter
  - 没有做多租户 checkpoint store
- 为什么：
  - 当前最合适的推进是先把 Shadow-ready 的最小桥接层做成独立 reference implementation，避免提前耦合到宿主细节。

### 本轮规划摘要

- 唯一主大任务：
  - writeback adapter reference implementation + checkpoint store 最小契约
- work package：
  - 实现 reference writeback adapter
  - 实现 in-memory checkpoint store
  - 让 runtime service 接入 adapter/store
  - 扩展验证、补文档和状态、提交主线

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：先做最小宿主 adapter stub，让 `docs / memory / graph` 各有一条 reference implementation

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 11/11 个关键子任务

### 执行结果

- 新增运行时代码：
  - `agentgraph/runtime/host_adapter.py`
    - `ReferenceWritebackAdapter`
    - `WritebackReceipt`
    - `BaseWritebackAdapter`
  - `agentgraph/runtime/checkpoint_store.py`
    - `InMemoryCheckpointStore`
    - `StoredCheckpointRecord`
    - `BaseCheckpointStore`
- 更新：
  - `agentgraph/runtime/service.py`
    - 支持注入 writeback adapter / checkpoint store
    - 结束运行后自动生成 `writeback_receipts`
    - checkpoint fallback 到 store
    - 支持 `register_request_context`
  - `agentgraph/runtime/__init__.py`
  - `agentgraph/__init__.py`
- 新增测试：
  - `tests/test_runtime_adapters.py`
    - docs/memory/graph 分桶写回
    - node override 与 reference 模式
    - 跨 service 实例的 checkpoint store resume
- 新增文档：
  - `docs/CHECKPOINT_STORE_CONTRACT.md`
- 更新文档：
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`
  - `docs/README.md`
  - `README.md`

### 成熟度推进

- AgentGraph 从“只有 writeback 契约”推进到了“已经有 reference adapter 和最小 checkpoint store”的阶段。
- 这意味着它现在已经具备 Shadow-ready 的桥接雏形，而不只是 contract 输出层。

### docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实“AgentGraph 是独立 runtime / schema / adapter 项目”
- `ADAPTER_BOUNDARY`
  - 已补 reference adapter 与 store 的默认基线
- `WRITEBACK_ADAPTER_CONTRACT`
  - 已补当前 reference implementation
- `CHECKPOINT_STORE_CONTRACT`
  - 新增并明确当前最小 store 边界

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `agentgraph/runtime/host_adapter.py`
  - `agentgraph/runtime/checkpoint_store.py`
  - `agentgraph/runtime/service.py`
  - `agentgraph/runtime/__init__.py`
  - `agentgraph/__init__.py`
  - `tests/test_runtime_adapters.py`
  - `docs/CHECKPOINT_STORE_CONTRACT.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`
  - `docs/README.md`
  - `README.md`
- 重点结论：
  - adapter/store 没有破坏现有 runtime contract
  - checkpoint store fallback 已打通跨 service 实例 resume 的最小链路
  - 当前残余风险集中在真实 Shadow adapter 绑定与上下文外部化

### 验证结果

- `pytest -q tests/test_runtime_adapters.py`
  - 3 passed
- `pytest -q`
  - 41 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\docs-gap-review.yaml`
  - valid=true
- reference adapter smoke
  - `status=succeeded; docs_artifacts=1; memory_checkpoints=2; receipts=3`

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1 周左右到 2 周。
- 依据：
  - 现在已经不只是在 contract 层可用，而是已经有最小桥接实现
  - 剩余更集中在真正 Shadow 绑定、request context 外部化和 checkpoint store 强化

### 下一轮建议

- 优先进入 `Shadow adapter binding`
- 再做 `request context` 外部化
- 然后补 `checkpoint store` 的 tenant / retention 最小边界

---

## 续推进补记 6

### 今日一句话补充

这轮把 Shadow 与 AgentGraph 的责任边界、拆分执行计划和一份专门面向人的中文解释正式落库了，后续不需要再靠零散对话回忆“谁负责什么”。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续“Shadow-ready bridge 已经具备，但真实集成责任需要明确切分”的主线判断。
- 完成上一轮哪部分：
  - 把责任矩阵、双计划拆分和中文解释文档正式写入仓库。
- 放弃上一轮哪部分：
  - 没有继续扩新 runtime 能力。
  - 没有开始真实 Shadow 接线实现。
- 为什么：
  - 当前最需要先固定的是责任边界与执行分工，否则下一轮很容易再次把 AgentGraph 与 Shadow 的责任混在一起。

### 本轮规划摘要

- 唯一主大任务：
  - 保存“责任矩阵 + 双计划拆分 + 中文解释”文档，并同步索引与状态
- work package：
  - 新增权威责任矩阵文档
  - 新增 Shadow / AgentGraph 双计划拆分
  - 新增面向人的中文解释版
  - 更新 docs 索引、daily brief 与 session memory

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：把 Shadow / AgentGraph 的责任边界和后续执行计划固化成仓库内权威资产

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 8/8 个关键子任务

### 执行结果

- 新增：
  - `docs/SHADOW_AGENTGRAPH_RESPONSIBILITY_MATRIX.md`
    - 固定 Shadow 与 AgentGraph 的责任边界
  - `docs/status/agent-plans/2026-03-23-shadow-agentgraph-split-plan.md`
    - 固定两边各自后续执行计划
  - `docs/status/agent-runs/2026-03-23-shadow-agentgraph-human-explainer.md`
    - 提供一份尽量少英文、面向人的中文解释
- 更新：
  - `docs/README.md`
    - 补责任矩阵入口
    - 补检查点存储入口
    - 补中文解释版入口
  - `docs/status/session-memory.md`
    - 记下责任边界已文档化、双计划已落库、中文解释已新增

### 成熟度推进

- AgentGraph 从“已经能接 Shadow，但责任边界主要靠对话说明”推进到“边界、计划、中文解释都已正式落库”的阶段。
- 这不是新能力扩张，但对后续集成推进非常关键，因为它减少了再度误判责任归属的风险。

### docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实“独立 runtime、宿主解耦”的方向
- `ADAPTER_BOUNDARY`
  - 其思想已被责任矩阵文档明确转译成执行边界
- `CHECKPOINT_STORE_CONTRACT`
  - 已被纳入给后续集成者的推荐阅读路径

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `docs/SHADOW_AGENTGRAPH_RESPONSIBILITY_MATRIX.md`
  - `docs/status/agent-plans/2026-03-23-shadow-agentgraph-split-plan.md`
  - `docs/status/agent-runs/2026-03-23-shadow-agentgraph-human-explainer.md`
  - `docs/README.md`
  - `docs/status/session-memory.md`
  - `docs/status/daily-briefs/2026-03-23-brief.md`
- 重点结论：
  - 责任矩阵、执行计划、中文解释三者定位清楚，没有互相替代
  - 中文解释版已明显降低英文术语密度，并对必要英文做了即时解释
  - `AGENTGRAPH_INTEGRATION.md` 没有被重新升格为主线入口

### 验证结果

- 责任矩阵文档已存在且路径正确
- 双计划拆分文档已存在且路径正确
- 中文解释文档已存在且路径正确
- `docs/README.md` 已能找到责任矩阵和中文解释版入口
- `git diff --check`
  - 待本轮统一校验

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1 周左右到 2 周。
- 依据：
  - 当前主干能力与最小桥接已经具备
  - 责任边界和执行计划也已写实
  - 剩余主要是 Shadow 真实接线和 AgentGraph 继续收紧公共接口

### 下一轮建议

- 优先进入 Shadow 的真实写回适配器与检查点存储实现
- AgentGraph 侧同步推进请求上下文正式接口
- 再补写回失败重试和检查点生命周期边界

---

## 续推进补记 4

### 今日一句话补充

`writeback.target / writeback.mode` 已经进入可场景化配置与失败路径自动化基线，AgentGraph 现在不只是“返回 writeback 字段”，而是开始真正约束宿主怎么消费它。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 artifact/checkpoint/writeback adapter 契约与 resume/adapter boundary 基线。
- 完成上一轮哪部分：
  - 把 writeback 从固定 `host/reference` 推进到三层优先级与失败路径校验。
- 放弃上一轮哪部分：
  - 没有进入 checkpoint store / multi-tenant。
  - 没有实现真实宿主持久化 adapter。
- 为什么：
  - 当前更高杠杆的是先把宿主消费语义、优先级和失败边界固定下来，避免后续 adapter 落地时契约继续漂移。

### 本轮规划摘要

- 唯一主大任务：
  - `writeback.target / writeback.mode` 宿主场景化 + writeback 失败路径验证基线
- work package：
  - 扩展 contract 校验到 workflow/request/node 三层
  - 在 runtime service 实现 writeback 优先级解析
  - 扩展官方样例与 HTTP/service 测试到失败路径
  - 回归验证、状态写回、主线提交

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：把 writeback target/mode 收紧成宿主可依赖的场景化契约，并验证失败路径

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 12/12 个关键子任务

### 执行结果

- 更新：
  - `agentgraph/runtime/contracts.py`
    - 校验 `workflow.defaults.writeback`
    - 校验 `RuntimeRequest.metadata.writeback`
    - 校验 `node.config.artifact.writeback`
  - `agentgraph/runtime/service.py`
    - 实现 writeback 三层优先级解析
    - artifact 支持 `target/mode` 场景化
    - checkpoint 支持 `target` 场景化
    - 新增 `mode=inline` 且无内容时的运行期失败
  - `agentgraph/server.py`
    - 运行期 `ValueError` 现在通过 HTTP `400` 返回，而不是混成 `500`
  - `examples/runtime-contract/docs-gap-review.yaml`
    - 场景化为 `docs + inline artifact / memory checkpoint`
  - `examples/runtime-contract/research-review-loop.yaml`
    - 场景化为 `docs artifact / memory checkpoint`
  - `examples/runtime-contract/parallel-synthesis.yaml`
    - 场景化为 `graph checkpoint`
  - `examples/runtime-contract/content-creation-phase1.yaml`
    - 增加 workflow 默认 writeback 与节点级 artifact override
  - `examples/runtime-contract/official-examples.yaml`
    - 补充官方样例 writeback 期望
  - `agentgraph/runtime/official_examples.py`
    - 补充官方样例 writeback 期望字段
  - `tests/test_runtime_examples.py`
    - 基于 workflow/request/node 三层优先级断言实际 writeback
  - `tests/test_runtime_contract.py`
    - 增加无效 target/mode、节点 override、inline 缺内容、HTTP 400/422 边界测试
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/WORKFLOW_SCHEMA.md`

### 成熟度推进

- AgentGraph 从“writeback 字段已经存在”推进到“writeback target/mode 已可配置、可验证、可失败”的阶段。
- 这让它更接近真正可集成的编排层，因为宿主现在不只是拿到对象，还能知道该把对象写回哪里、以什么模式写回，以及哪些配置会被拒绝。

### docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实“先收敛 runtime 主线，再谈外围扩张”，因为这轮主成果仍在 runtime contract 和 adapter boundary
- `RUNTIME_CONTRACT_SPEC`
  - 已补 writeback target/mode 和三层优先级
- `WORKFLOW_SCHEMA`
  - 已明确 `defaults.writeback` 和 `node.config.artifact.writeback`
- `WRITEBACK_ADAPTER_CONTRACT`
  - 已从静态默认值说明，升级为场景化契约与失败语义说明

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `agentgraph/runtime/contracts.py`
  - `agentgraph/runtime/service.py`
  - `agentgraph/server.py`
  - `agentgraph/runtime/official_examples.py`
  - `examples/runtime-contract/*.yaml`
  - `tests/test_runtime_examples.py`
  - `tests/test_runtime_contract.py`
  - `docs/WRITEBACK_ADAPTER_CONTRACT.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/WORKFLOW_SCHEMA.md`
- 重点结论：
  - writeback 优先级已经有清晰的 contract 入口与 runtime 行为
  - 失败路径没有被吞掉，HTTP 层也能正确区分 `422` 和 `400`
  - checkpoint/resume 没有被新的 writeback 场景化破坏

### 验证结果

- `pytest -q`
  - 38 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\content-creation-phase1.yaml`
  - valid=true
- `python -m agentgraph.cli run -w examples\runtime-contract\docs-gap-review.yaml -i '{"goal":"Analyze docs coverage and produce review notes"}'`
  - succeeded

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1 周左右到 2 周。
- 依据：
  - 现在 runtime contract、官方样例、resume、adapter boundary、writeback 场景化和失败路径都已进入默认基线
  - 剩余更集中在“真正的宿主 adapter 实现”和“checkpoint store / tenant scope”这类收尾能力

### 下一轮建议

- 优先实现一个最小宿主 writeback adapter stub / reference implementation
- 再进入 checkpoint store / tenant scope 最小契约
- 如需继续加验证，优先覆盖 writeback retry / failure handling 语义
