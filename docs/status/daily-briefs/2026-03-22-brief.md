# 2026-03-22 AgentGraph Brief

## 今日一句话总结

把 Runtime Contract Campaign 从文档层推进到了代码入口层，首次建立了 CLI / FastAPI 共用的 runtime contract 与执行服务。

## 上一轮进展承接

- 延续上一轮什么：
  - 延续 `CORE_CHARTER` 和 `RUNTIME_CONTRACT_SPEC` 已经明确的 Phase 1 主线，即先收敛独立编排 runtime，再谈配套模块。
- 完成上一轮哪部分：
  - 把文档里冻结的核心对象正式落为代码模型，并打通入口层共享执行路径。
- 放弃上一轮哪部分：
  - 没有去补旧 `SwarmRouter`、旧 memory 全量恢复、前端节点格式兼容层等历史分支。
- 为什么：
  - 这些能力会继续拉宽边界，不能帮助 AgentGraph 先成为可独立调用的黑盒编排 runtime。

## 本轮规划摘要

- 唯一主大任务：Runtime contract 与代码实现对齐。
- 次级战役：状态闭环最小建制化。
- campaign -> work package -> subtask：
  - Runtime Contract Campaign -> 统一 contract 模型 -> 新增 workflow/request/run/step/artifact/checkpoint 模型
  - Runtime Contract Campaign -> 同构入口收敛 -> CLI 与 FastAPI 共用 `RuntimeService`
  - Runtime Contract Campaign -> 验证与状态闭环 -> 契约测试 + `session-memory` + 今日 brief

## 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 主大任务：让 `RUNTIME_CONTRACT_SPEC` 成为真正可执行、可验证、可被入口层消费的代码契约

## 工作包与子任务完成统计

- 已完成 3/3 个工作包
- 已完成 7/7 个关键子任务

## 执行结果

- 新增 `agentgraph/runtime/contracts.py`
  - 定义 `WorkflowDefinition`、`RuntimeRequest`、`RunRecord`、`StepRecord`、`ArtifactRef`、`CheckpointRef`、`RunResult`
- 新增 `agentgraph/runtime/service.py`
  - 提供统一 `validate_workflow`、`run`、`get_run`
  - 支持基础顺序/条件终止执行路径与 step/checkpoint/artifact 产出
- 更新 `agentgraph/cli.py`
  - 新增 `agentgraph validate`
  - `agentgraph run` 改为走统一 contract/service
- 更新 `agentgraph/server.py`
  - 提供 `POST /workflow/validate`
  - 提供 `POST /workflow/run`
  - 提供 `GET /runs/{id}`
- 更新顶层导出
  - 增加 runtime 相关导出
  - 对 SQLite/Redis optional import 做最小保护
- 新增契约测试
  - 验证 runtime service 的结果结构
  - 验证 FastAPI 三个核心端点

## 成熟度推进

- AgentGraph 从“只有 runtime contract 文档草案”推进到“已经有统一 contract 代码骨架和同构入口路径”的阶段。
- 这是从概念收敛走向黑盒可调用 runtime 的第一段实质性工程推进。

## docs 思想落实进度

- `CORE_CHARTER`
  - 已落实“先收敛 runtime，再扩张叙事”
  - 已落实“CLI / HTTP API 同构入口”
- `RUNTIME_CONTRACT_SPEC`
  - 已落实 7 个核心对象中的主体模型
  - 已落实 `run -> step -> final_output -> trace -> artifacts -> checkpoints`
- `WHAT_IS_AGENTGRAPH`
  - 已落实“Python 包 / CLI / HTTP runtime 服务”中的 CLI 与 HTTP 入口收敛
- `agentgraph-phase1-campaign-draft`
  - 已落实第一阶段 MVP 里的 validate/run/get_run 主路径雏形

## 代码审查摘要

- 本轮只审查新增/改动文件：
  - `agentgraph/runtime/contracts.py`
  - `agentgraph/runtime/service.py`
  - `agentgraph/cli.py`
  - `agentgraph/server.py`
  - `agentgraph/__init__.py`
  - `tests/test_runtime_contract.py`
- 重点结论：
  - 契约方向与核心 docs 一致，没有再把前端节点格式当 canonical schema
  - CLI 与 HTTP 执行链已统一，不再各自拼装 graph
  - 仍存在集成风险：旧包导出与历史测试体系尚未完全围绕新 contract 收口

## 验证结果

- 已新增针对本轮主线的 contract tests
- 预期验证：
  - runtime service step/checkpoint/artifact 结构
  - FastAPI validate/run/get_run 入口
- 基线现状：
  - 历史全量 `pytest` 仍被 `aiosqlite` 缺失和旧 router 接口漂移阻断，说明旧测试基线尚未完成 Phase 1 对齐

## 时间进度判断

- 按当前节奏，距离“独立、稳定、可集成的多智能体编排层”预计还需 4-6 周。
- 依据：
  - 现在已拿下契约骨架和入口同构第一步
  - 但 canonical schema 文档回写、示例迁移、旧导出/依赖隔离、checkpoint 恢复、测试矩阵收口还未完成

## 下一轮建议

- 优先把示例 workflow 与文档 schema 迁移到新 contract
- 然后收敛包导出和依赖边界，剥离旧接口造成的测试噪音
- 之后再进入 checkpoint 恢复与 adapter 边界细化

---

## 续推进补记

### 今日一句话补充

canonical workflow schema、两份官方端到端样例和主 README/教程已经全部切到 Phase 1 contract 主线。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 runtime contract 骨架和 CLI / HTTP 共用执行路径。
- 完成上一轮哪部分：
  - 把“下一轮建议”里的 schema 文档和官方样例收敛真正做完了。
- 放弃上一轮哪部分：
  - 没有清理 legacy 示例全集，也没有修复全量历史测试。
- 为什么：
  - 先满足“至少有 2 个可重复执行的端到端样例”这个独立成立标准，价值高于继续扫旧噪音。

### 本轮规划摘要

- 唯一主大任务：canonical workflow schema 与正式样例收敛
- work package：
  - schema 文档落地
  - 官方样例迁移
  - README / 教程切换
  - 端到端验证

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：让官方 schema、官方样例、CLI 文档和验证链全部指向同一套 canonical contract

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 8/8 个关键子任务

### 执行结果

- 新增 `docs/WORKFLOW_SCHEMA.md`
- 新增官方样例：
  - `examples/runtime-contract/docs-gap-review.yaml`
  - `examples/runtime-contract/research-review-loop.yaml`
- 重写：
  - `README.md`
  - `examples/README.md`
  - `docs/tutorials/getting-started/your-first-workflow.md`
- 新增：
  - `tests/test_runtime_examples.py`
- 修复：
  - `agentgraph/runtime/service.py` 保留根输入字段，避免多步节点读取不到初始 `goal`

### 成熟度推进

- AgentGraph 从“有 contract 骨架”推进到“有权威 schema 文档 + 两个可重复官方样例 + 文档入口统一”的阶段。
- 这让独立成立标准里的第 1、3、6 条进一步从草案走向可验证状态。

### docs 思想落实进度

- `CORE_CHARTER`
  - 已落实 canonical workflow definition 与 2 个官方样例
- `RUNTIME_CONTRACT_SPEC`
  - 已落实 workflow / run / step / artifact / checkpoint 的样例级验证
- `WHAT_IS_AGENTGRAPH`
  - 已落实 CLI 可直接消费官方样例
- `agentgraph-phase1-campaign-draft`
  - 已更接近 MVP 中的“2 个端到端示例 workflow”

### 代码审查摘要

- 本轮只审查新增/改动文件：
  - `docs/WORKFLOW_SCHEMA.md`
  - `examples/runtime-contract/*.yaml`
  - `README.md`
  - `examples/README.md`
  - `docs/tutorials/getting-started/your-first-workflow.md`
  - `tests/test_runtime_examples.py`
  - `agentgraph/runtime/service.py`
- 重点结论：
  - 官方样例与当前 runtime contract 已一致
  - README / 教程不再继续放大旧 legacy schema
  - 输入在多步执行链中的保留问题已修复

### 验证结果

- `pytest -q tests\\test_runtime_contract.py tests\\test_runtime_examples.py`
  - 4 passed
- `python -m agentgraph.cli validate -w examples\\runtime-contract\\docs-gap-review.yaml`
  - valid=true
- `python -m agentgraph.cli run -w examples\\runtime-contract\\research-review-loop.yaml -i '{"goal":"Produce a research review"}'`
  - succeeded

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 3-5 周。
- 依据：
  - 现在 contract、入口、schema、官方样例四件套已经接上
  - 剩余主要是 legacy 收敛、checkpoint 恢复、依赖边界和测试基线清理

### 下一轮建议

- 继续只保留一个主大任务：
  - legacy 示例 / legacy 文档 / legacy 测试基线收敛
- 若还要继续推进 runtime 主线，优先做：
  - checkpoint 恢复模型细化
  - adapter boundary 文档与返回结构稳定化

---

## 续推进补记 2

### 今日一句话补充

权威入口与 legacy 入口已经被显式分开，旧 HTTP API 叙事和高风险 legacy 示例不再伪装成当前 Phase 1 官方能力。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 canonical schema、官方样例、统一 CLI/HTTP 入口。
- 完成上一轮哪部分：
  - 完成了 legacy 边界显式化与主线隔离的第一轮收口。
- 放弃上一轮哪部分：
  - 没有全面迁移所有 legacy 文档和 legacy 测试。
- 为什么：
  - 优先降低误导性最高的入口层风险，比立即扫完整个历史面更能保护当前主线。

### 本轮规划摘要

- 唯一主大任务：legacy 边界显式化与主线隔离
- work package：
  - 权威入口文档收敛
  - HTTP API 文档改写到当前 contract
  - 高风险 legacy 教程/示例显式标记
  - 回归验证

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：让 canonical contract 与 legacy surface 在文档和示例层彻底分开

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 7/7 个关键子任务

### 执行结果

- 新增 `docs/LEGACY_SURFACE_MAP.md`
- 重写 `docs/api/http/README.md`
- 更新 `docs/README.md`
- 更新 legacy 教程：
  - `docs/tutorials/multi-agent/creating-cooperative-agents.md`
- 更新高风险 legacy 示例标记：
  - `examples/simple_workflow.yaml`
  - `examples/multi-agent/content-creation-workflow.yaml`
  - `examples/complex-workflows/data-processing-pipeline.yaml`
  - `examples/integrations/github-monitoring-workflow.yaml`
  - `examples/templates/*.yaml`

### 成熟度推进

- AgentGraph 从“已有 canonical schema 与官方样例”进一步推进到“权威入口与 legacy surface 已可区分”的阶段。
- 这减少了后续开发、集成和文档继续沿旧 schema 漂移的风险。

### docs 思想落实进度

- `CORE_CHARTER`
  - 已继续落实“先收敛 runtime，再扩张叙事”
- `RUNTIME_CONTRACT_SPEC`
  - 已通过 HTTP API 文档改写进一步落实当前真实支持边界
- `WHAT_IS_AGENTGRAPH`
  - 已继续落实“独立 runtime，而非大而全平台”
- `agentgraph-phase1-campaign-draft`
  - 已进一步落实“文档能说清已支持 / 未支持 / 延后再做”

### 代码审查摘要

- 本轮只审查新增/改动文件：
  - `docs/LEGACY_SURFACE_MAP.md`
  - `docs/api/http/README.md`
  - `docs/README.md`
  - `docs/tutorials/multi-agent/creating-cooperative-agents.md`
  - 若干高风险 legacy YAML
- 重点结论：
  - 当前权威入口已清晰
  - 当前未实现 HTTP API 能力已不再被权威文档错误宣称
  - legacy 示例被显式标注，集成误导风险下降

### 验证结果

- `pytest -q tests\\test_runtime_contract.py tests\\test_runtime_examples.py`
  - 4 passed
- `python -m agentgraph.cli validate -w examples\\runtime-contract\\docs-gap-review.yaml`
  - valid=true
- 文档检索：
  - 权威 docs 范围内已不再检出旧 `POST /workflows`、`GET /workflows`、Bearer token、`agentgraph list`、`interactive -w` 入口描述

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 3-4 周。
- 依据：
  - 现在 contract、入口、schema、官方样例、legacy 边界五件套都已成形
  - 剩余主要集中在 legacy 测试基线、旧导出边界、checkpoint 恢复与 adapter boundary

### 下一轮建议

- 下一轮继续只保留一个主大任务：
  - legacy 测试基线与旧导出边界收敛
- 如果要继续贴 runtime 主线推进：
  - checkpoint 恢复模型
  - adapter boundary 文档与结果稳定化

---

## 续推进补记 3

### 今日一句话补充

默认 `pytest -q` 已经被收敛为 Phase 1 contract 基线并通过，历史测试噪音不再阻断主线推进。

### 上一轮进展承接

- 延续上一轮什么：
  - 延续 legacy 边界显式化和权威入口隔离。
- 完成上一轮哪部分：
  - 完成了主线测试基线与导入边界的第一轮硬收口。
- 放弃上一轮哪部分：
  - 没有修复所有 legacy 测试，只做了分层和隔离。
- 为什么：
  - 这是压缩开发周期的高杠杆动作，优先级高于逐个修旧测试。

### 本轮规划摘要

- 唯一主大任务：legacy 测试基线与旧导出边界收敛
- work package：
  - optional import 继续收薄
  - 默认 pytest 切到 contract baseline
  - Python API / integration 高风险入口降级为 legacy notice
  - 回归验证

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：建立稳定的 Phase 1 contract-only 基线，降低每轮推进的固定噪音成本

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 8/8 个关键子任务

### 执行结果

- 更新 `agentgraph/memory/__init__.py`
  - 继续把 `UserMemory`、`GlobalMemory`、`PatternManager` 等改成 optional import
- 新增 `tests/conftest.py`
  - 默认只收集 Phase 1 baseline tests
  - legacy tests 需显式 `--run-legacy`
- 更新 `pyproject.toml`
  - 去掉当前环境无效的 `asyncio_mode`
- 重写：
  - `docs/api/python/AgentGraph.md`
  - `docs/api/python/Memory.md`
- 更新：
  - `docs/AGENTGRAPH_INTEGRATION.md` 顶部 legacy notice

### 成熟度推进

- AgentGraph 从“主线测试可单独跑”推进到“默认测试入口就是主线 contract 基线”的阶段。
- 这会显著减少后续 3-4 周里每一轮被历史测试打断的成本，是典型的高杠杆基础设施收口。

### docs 思想落实进度

- `CORE_CHARTER`
  - 已继续落实“对外黑盒，对内可演进”
- `RUNTIME_CONTRACT_SPEC`
  - 已继续落实“CLI / HTTP / tests 围绕统一 contract”
- `WHAT_IS_AGENTGRAPH`
  - 已通过 Python API 文档改写，进一步把主线从旧 graph API 拉回 runtime contract
- `agentgraph-phase1-campaign-draft`
  - 已更接近“可重复执行端到端样例 + 稳定验证链”

### 代码审查摘要

- 本轮只审查新增/改动文件：
  - `agentgraph/memory/__init__.py`
  - `tests/conftest.py`
  - `pyproject.toml`
  - `docs/api/python/AgentGraph.md`
  - `docs/api/python/Memory.md`
  - `docs/AGENTGRAPH_INTEGRATION.md`
- 重点结论：
  - memory optional dependency 泄漏继续下降
  - 默认测试入口已与 Phase 1 主线一致
  - Python API 与集成文档的高风险误导入口已被降级

### 验证结果

- `pytest -q`
  - 4 passed
- `python -m agentgraph.cli validate -w examples\\runtime-contract\\research-review-loop.yaml`
  - valid=true

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 2-4 周。
- 若继续采用并行子代理 + 高杠杆主战役的方式推进，有机会进一步压到约 1-2 周的密集收口。
- 依据：
  - 现在已经不再被默认测试入口和文档入口持续拖慢
  - 剩余工作更集中在少数高价值实现面：checkpoint、adapter boundary、legacy 迁移

### 下一轮建议

- 下一轮继续只保留一个主大任务：
  - checkpoint 恢复模型与 adapter boundary
- 如需继续压缩周期：
  - 并行安排 legacy 测试迁移
  - 并行安排 Python API / integration 文档彻底主线化

## 续推进补记 4

### 今日一句话总结

- AgentGraph 已从上层仓库中的未跟踪目录，正式收敛为自己的独立 Git 仓库，并在 `main` 主线分支上完成提交前验证。

### 上一轮进展承接

- 承接了上一轮已经完成的：
  - checkpoint 恢复模型
  - adapter boundary 文档
  - contract-only 默认测试基线
- 本轮不再扩展新能力，而是把这些已验证成果真正落到 AgentGraph 自己的主线仓库。

### 本轮规划摘要

- 唯一主大任务：
  - 独立仓库初始化、主线分支规范化、提交前验证、首个主线提交

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：完成 AgentGraph 自身仓库边界初始化，并将已验证主线成果提交到 `main`

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 7/7 个关键子任务

### 执行结果

- 初始化独立仓库：
  - `git init` 已在 `D:\VScode\TotalProject\AgentGraph` 完成
- 新增：
  - `.gitignore`
- 收敛忽略项：
  - Python cache / pytest cache / build artifacts
  - `node_modules/`
  - `src-tauri/target/`
  - `.opencode/`
  - `nul.okm`
- 主线分支统一为：
  - `main`
- 写回状态：
  - `docs/status/session-memory.md`
  - `docs/status/daily-briefs/2026-03-22-brief.md`

### 成熟度推进

- AgentGraph 从“代码与文档主线已收敛”推进到“版本控制主线也已独立成立”的阶段。
- 这意味着后续协作、发布、远程仓库绑定、PR 和 release 基线终于可以围绕 AgentGraph 自身展开，而不再混入上层目录语义。

### docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实“独立、稳定、可集成编排层”中的“独立”部分
- `WHAT_IS_AGENTGRAPH`
  - 不再只是逻辑上的独立 runtime，仓库边界也开始独立
- `agentgraph-phase1-campaign-draft`
  - “形成权威主线并可持续迭代”的前置条件已补齐

### 代码审查摘要

- 本轮只审查与仓库主线提交直接相关文件：
  - `.gitignore`
  - `docs/status/session-memory.md`
  - `docs/status/daily-briefs/2026-03-22-brief.md`
- 结论：
  - 忽略规则保守，没有误排除源码、文档、测试与样例
  - 提交前状态文件已完整记录仓库边界变化与验证结果

### 验证结果

- `pytest -q`
  - 5 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\research-review-loop.yaml`
  - valid=true
- FastAPI `run -> checkpoint -> resume` smoke
  - resume-smoke-ok

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 2-4 周。
- 但“独立仓库 + main 主线”这个前置条件已经补齐，后续推进的组织成本会明显下降。

### 下一轮建议

- 下一轮最自然继续：
  - legacy 示例与 legacy 测试迁移
  - adapter boundary 进一步固化
  - 远程仓库绑定 / release 基线（如果需要对外协作）

## 续推进补记 5

### 今日一句话总结

- AgentGraph 已新增 Phase 1 可验证的基础并行能力：`control.parallel` fan-out 与 `control.barrier` join。

### 上一轮进展承接

- 承接了上一轮已经完成的：
  - 独立仓库与 `main` 主线
  - checkpoint 恢复
  - contract-only 默认测试基线
- 本轮继续围绕 `Runtime Contract Campaign` 推进，目标是补齐 Phase 1 成立标准中仍缺的一块：基础并行。

### 本轮规划摘要

- 唯一主大任务：
  - 实现 Phase 1 最小并行 fan-out / barrier 能力，并同步收口 schema、文档、样例、测试

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：基础并行 fan-out + barrier join 收敛

### 工作包与子任务完成统计

- 已完成 5/5 个工作包
- 已完成 10/10 个关键子任务

### 执行结果

- 更新：
  - `agentgraph/runtime/contracts.py`
    - 新增 `control.parallel` / `control.barrier` 约束校验
  - `agentgraph/runtime/service.py`
    - 新增 fan-out / barrier 状态迁移
    - 支持并行分支后的 barrier 汇总
    - 并行节点 warning 逻辑纠偏
  - `docs/WORKFLOW_SCHEMA.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `examples/README.md`
  - `README.md`
- 新增：
  - `examples/runtime-contract/parallel-synthesis.yaml`
- 扩展测试：
  - `tests/test_runtime_contract.py`
    - 并行执行测试
    - 并行 checkpoint 恢复测试
  - `tests/test_runtime_examples.py`
    - 纳入新官方样例

### 成熟度推进

- AgentGraph 从“可执行串行 / 条件 / checkpoint 恢复的 contract runtime”推进到“具备基础 fan-out / barrier 能力的 contract runtime”。
- 这使它更接近 Phase 1 文档里要求的独立编排层成立标准，而不再只是顺序工作流执行器。

### docs 思想落实进度

- `CORE_CHARTER`
  - 落实了“先收敛 runtime，再扩张叙事”，因为这轮补的是 runtime 主线能力
- `RUNTIME_CONTRACT_SPEC`
  - 已把基础并行 / barrier 纳入当前最小控制流能力
- `WORKFLOW_SCHEMA`
  - 已新增 fan-out / barrier 的 canonical schema 说明
- `ADAPTER_BOUNDARY`
  - 已开始明确宿主如何理解最小并行边界

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `agentgraph/runtime/contracts.py`
  - `agentgraph/runtime/service.py`
  - `docs/WORKFLOW_SCHEMA.md`
  - `docs/RUNTIME_CONTRACT_SPEC.md`
  - `docs/ADAPTER_BOUNDARY.md`
  - `examples/runtime-contract/parallel-synthesis.yaml`
  - `examples/README.md`
  - `README.md`
  - `tests/test_runtime_contract.py`
  - `tests/test_runtime_examples.py`
- 重点结论：
  - 新能力仍是 contract-first，而非绑定线程池或特定 worker
  - checkpoint 恢复链没有被并行能力破坏
  - 通过 validator 把 Phase 1 不支持的多层并行复杂度挡在 contract 之外

### 验证结果

- `pytest -q`
  - 7 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\parallel-synthesis.yaml`
  - valid=true
  - warnings=[]
- runtime parallel smoke
  - parallel-smoke-ok

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1.5-3 周。
- 依据：
  - 现在已经具备串行、条件分支、基础并行、checkpoint 恢复、CLI/HTTP 同构入口、官方样例和默认测试基线
  - 剩余更集中在 legacy surface 收敛、adapter 消费边界细化和更完整验证矩阵

### 下一轮建议

- 下一轮继续只保留一个主大任务：
  - legacy 示例 / legacy 测试 / legacy 文档继续收敛
- 如需继续紧贴 runtime 主线：
  - 细化 barrier 输出消费约定
  - 细化 checkpoint 恢复与 adapter 消费文档

## 续推进补记 6

### 今日一句话总结

- AgentGraph 已把默认入口层进一步从旧 graph/memory 叙事收回到 runtime contract 主线。

### 上一轮进展承接

- 承接了上一轮已经完成的：
  - 基础 parallel / barrier runtime
  - checkpoint 恢复
  - 官方样例与默认 contract baseline
- 本轮不再扩展新 runtime 功能，而是集中推进 `legacy surface 收敛`。

### 本轮规划摘要

- 唯一主大任务：
  - 收敛 legacy 文档、legacy Python API、legacy 测试入口和高风险旧示例提示

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：legacy surface 收敛

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 9/9 个关键子任务

### 执行结果

- 更新：
  - `agentgraph/__init__.py`
    - 顶层包描述改为 runtime-contract first
    - `WorkflowDefinition` / `RuntimeRequest` / `ResumeRequest` / `RuntimeService` / `RunResult` 前置
  - `docs/api/python/AgentGraph.md`
  - `docs/api/python/Agent.md`
  - `docs/api/python/Memory.md`
    - 三页都已改成 legacy 说明页
  - `docs/README.md`
    - Python API 历史页与旧教程已移到 legacy 分组
    - runtime / integration 推荐阅读路径已更贴近 contract 主线
  - `docs/WHAT_IS_AGENTGRAPH.md`
    - Python 使用示例改成 runtime 入口
  - `tests/conftest.py`
    - 显式维护 `LEGACY_TEST_FILES`
  - `pyproject.toml`
    - 新增 `legacy` pytest marker 说明
  - `tests/test_*.py`
    - 多个 legacy 测试文件头补 `pytest.mark.legacy`
  - `docs/tutorials/advanced/custom-node-development.md`
  - `docs/tutorials/multi-agent/creating-cooperative-agents.md`
  - `examples/simple_workflow.yaml`
  - `examples/multi-agent/content-creation-workflow.yaml`
    - 增强 canonical 跳转提示
- 新增：
  - `tests/README.md`

### 成熟度推进

- AgentGraph 从“主线能力已成形，但默认入口仍可能把人带回旧 API”推进到“默认入口更稳定地指向 runtime contract”的阶段。
- 这会直接降低后续新贡献、新集成和新自动化误踩 legacy surface 的概率。

### docs 思想落实进度

- `CORE_CHARTER`
  - 继续落实“先收敛 runtime，再扩张叙事”
- `WHAT_IS_AGENTGRAPH`
  - Python 使用方式已与当前 runtime contract 更一致
- `LEGACY_SURFACE_MAP`
  - 从“指出问题”推进到“默认入口已开始按该图谱真正收口”

### 代码审查摘要

- 本轮只审查本轮改动文件。
- 重点结论：
  - 没有破坏 runtime contract 或 CLI / HTTP 路径
  - 主要收益来自入口层纠偏和测试分层显式化
  - 风险仍在于 `docs/AGENTGRAPH_INTEGRATION.md` 等历史大文档正文尚未完全瘦身，但它们已不再占据主线入口位

### 验证结果

- `pytest -q`
  - 7 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\parallel-synthesis.yaml`
  - valid=true
  - warnings=[]
- top-level import smoke
  - import-smoke-ok

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1-2.5 周。
- 依据：
  - 主线能力已经覆盖串行、条件、基础并行、checkpoint、CLI/HTTP、官方样例、默认测试基线
  - 现在连默认文档和默认包入口都在往同一条 contract 主线收敛

### 下一轮建议

- 下一轮继续只保留一个主大任务：
  - 历史大文档与集成叙事继续瘦身
- 如果继续贴着 runtime 主线推进：
  - 细化 adapter 消费约定
  - 评估 `tests/legacy/` 目录化迁移

## 续推进补记 7

### 今日一句话总结

- AgentGraph 的高噪音历史大文档已经开始成批收敛为明确的 legacy / concept note，不再像当前主线实施指南。

### 上一轮进展承接

- 承接了上一轮已经完成的：
  - 顶层包入口 runtime-first 化
  - Python API 历史页降级
  - 测试分层显式化
- 本轮继续沿着 `legacy surface 收敛` 主战役前进，把最容易误导新读者的历史大文档继续瘦身。

### 本轮规划摘要

- 唯一主大任务：
  - 历史大文档与概念叙事入口瘦身
- 执行方式：
  - 多智能体并行分工，按文件切片协作

### 本轮主大任务 / 主战役

- 主战役：Runtime Contract Campaign
- 本轮主大任务：历史大文档与集成叙事瘦身

### 工作包与子任务完成统计

- 已完成 4/4 个工作包
- 已完成 8/8 个关键子任务

### 执行结果

- 多智能体分工：
  - Worker 1：`docs/AGENTGRAPH_INTEGRATION.md`
  - Worker 2：`docs/PHASE0_SUMMARY.md`
  - Worker 3：`docs/CLI_ANYTHING_RELATION.md` + `examples/ai-code-assistant/README.md`
  - Main：`docs/README.md` + `docs/LEGACY_SURFACE_MAP.md` + 验证 + 状态写回
- 更新：
  - `docs/AGENTGRAPH_INTEGRATION.md`
    - 收敛为 legacy integration note
  - `docs/PHASE0_SUMMARY.md`
    - 收敛为 legacy summary
  - `docs/CLI_ANYTHING_RELATION.md`
    - 收敛为 legacy / concept note
  - `examples/ai-code-assistant/README.md`
    - 收敛为 concept demo note
  - `docs/README.md`
    - 补充历史大文档/概念叙事边界
  - `docs/LEGACY_SURFACE_MAP.md`
    - 纳入高噪音历史大文档清单

### 成熟度推进

- AgentGraph 从“默认入口已大体收口”推进到“历史大文档也开始被系统性降噪”的阶段。
- 这意味着新读者和新集成方更不容易把历史概念文档误当成当前 contract 主线。

### docs 思想落实进度

- `CORE_CHARTER`
  - 更接近“独立、稳定、可集成”的文档治理状态
- `LEGACY_SURFACE_MAP`
  - 已从示例/教程层扩展到历史大文档层
- `WHAT_IS_AGENTGRAPH`
  - 主线产品叙事和历史背景叙事正在逐步分离

### 代码审查摘要

- 本轮只审查本轮改动文件：
  - `docs/AGENTGRAPH_INTEGRATION.md`
  - `docs/PHASE0_SUMMARY.md`
  - `docs/CLI_ANYTHING_RELATION.md`
  - `examples/ai-code-assistant/README.md`
  - `docs/README.md`
  - `docs/LEGACY_SURFACE_MAP.md`
- 重点结论：
  - 改动主要是“历史定位收缩”，没有重新扩展旧 API 或旧集成叙事
  - 通过删减大量可照抄旧实现段落，降低了误导风险

### 验证结果

- `pytest -q`
  - 7 passed
- `python -m agentgraph.cli validate -w examples\runtime-contract\parallel-synthesis.yaml`
  - valid=true
  - warnings=[]

### 时间进度判断

- 按当前节奏，距离成为独立、稳定、可集成编排层预计还需 1-2 周。
- 依据：
  - 主线能力和主线入口已经都相对稳定
  - 剩余工作越来越集中在少数历史资产和 adapter 细节收口

### 下一轮建议

- 下一轮继续只保留一个主大任务：
  - 剩余历史大文档与 legacy tests 目录化收口
- 如果继续采用多智能体协作：
  - 可以按“单文件单 worker”的切片方式继续推进
