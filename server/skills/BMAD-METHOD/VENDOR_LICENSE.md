# Vendored skill — BMAD-METHOD

This built-in skill is vendored from an upstream open-source project.

- **Upstream:** https://github.com/bmadcode/BMAD-METHOD
- **License:** MIT
- **Vendored on:** 2026-06-03
- **How it got here:** originally ingested via `POST /api/skills/ingest`
  (lived under `.shadowflow/skills/` as a *user* skill), then **promoted** to a
  built-in by moving the directory into `server/skills/` (the built-in scan
  root). The loader tags `source: 'builtin'` by scan position; `references/`
  holds the upstream repo content the skill-compiler reads to build the team.

The original MIT copyright and permission notice belong to the BMAD-METHOD
authors. This vendored copy redistributes their work under the same MIT terms.
See the upstream repository's `LICENSE` for the full text.
