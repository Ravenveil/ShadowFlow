---
name: Team-level MCP server schema + API + 凭证管理
status: open
created: 2026-05-26T14:20:03Z
updated: 2026-05-26T14:20:03Z
depends_on: []
parallel: false
conflicts_with: []
priority: P0
size: M
hours: 16-20
---

# Task: Team-level MCP server schema + API + 凭证管理

## Description

把"team 可挂哪些 MCP server"做成一等公民。schema + CRUD + 凭证 ref（不内联 secrets）。

## Acceptance Criteria

- [ ] `TeamMCPServerSpec` Pydantic 模型：
  - `id, server_url, transport: 'stdio'|'http'|'sse', credentials_ref?: string,
    allowed_tools: List[str], permission: 'allow'|'ask'|'deny'`
- [ ] `team.mcp_servers: List[TeamMCPServerSpec]` 加入持久化
- [ ] `GET/PUT /api/teams/{team_id}/mcp-servers` endpoints
- [ ] 凭证 ref 走现有 secrets store（BYOK config 模式），不允许内联
- [ ] 单测覆盖

## Technical Details

- 复用 `ToolPolicy.permission_rules`（contracts_builder.py:93）的 allow/ask/deny 模型
- 沿用 `shadowflow/api/teams.py` 现有 CRUD 模式

## Dependencies

- 无前置

## Effort Estimate

- Size: M
- Hours: 16-20
- Parallel: false（blocks 002）

## Definition of Done

- [ ] 单测全绿
- [ ] curl 验证 endpoints
