---
name: team_workflow.yaml schema + Phase 1/2/3 适配
status: open
created: 2026-05-26T14:20:03Z
updated: 2026-05-26T14:20:03Z
depends_on: []
parallel: false
conflicts_with: []
priority: P0
size: L
hours: 24-40
---

# Task: team_workflow.yaml schema + Phase 1/2/3 适配

## Description

把当前"LLM 每 turn 现编 DAG"升级为"用户可编辑、可版本控制的 team_workflow.yaml"。
缺省时仍走现编路径（向后兼容）。

## Acceptance Criteria

- [ ] `TeamWorkflowDefinition` Pydantic 模型，含：
  - `stages: List[Stage]` —— 每 stage 含 `name`, `default_agent`, `must_produce`,
    `advance_when`, `rollback_to`
  - `transitions: List[Transition]` —— 阶段间推进 / 回滚显式声明
- [ ] `shadowflow/api/teams.py` 的 `TeamWorkflow` 扩展支持 yaml 格式（nodes/edges 之外）
- [ ] Phase 1/2/3 pipeline 入口：先查 team_workflow.yaml，缺省再走 LLM 现编 DAG
- [ ] 提供 1 个种子 yaml 示例（参考文章 PM → 需求 → 设计 → Gatekeeper → Dev → Review → QA）

## Technical Details

- 复用 `shadowflow/api/teams.py:247 TeamWorkflow` schema 但需扩展
- 与 Policy Matrix（retry/double-reject/strict）正交：workflow 管"阶段走向"，policy 管"重试行为"

## Dependencies

- 无前置

## Effort Estimate

- Size: L
- Hours: 24-40
- Parallel: false（blocks 002）

## Definition of Done

- [ ] 集成测试覆盖"加载 yaml → Phase 1/2/3 按 yaml 跑"
- [ ] 一个种子 yaml 示例可加载执行
