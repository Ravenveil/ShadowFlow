---
name: Runtime 执行点接入
status: open
created: 2026-05-26T14:20:03Z
updated: 2026-05-26T14:20:03Z
depends_on: [002]
parallel: false
conflicts_with: []
priority: P0
size: M
hours: 16-24
---

# Task: Runtime 执行点接入

## Description

在 turn 主循环 `<sf:complete>` 之后、标 done 之前插入"必跑 validation hook"环节，按
on_fail 行为决定走 retry 或 blocker 路径。

## Acceptance Criteria

- [ ] 找到 turn 完成的代码位置（candidates: `shadowflow/runtime/turn_executor.py` /
      `server/src/routes/run-sessions.ts` parser hook）
- [ ] 在标 done 之前调用 `validation_hooks_runner.run_all(team_id, turn_ctx)`
- [ ] 所有 hook 全过 → turn 标 done；任一 fail → 按 on_fail 走 retry 或 blocker
- [ ] SSE 推送新事件 `<sf:validation>` 含每个 hook 的 pass/fail + reason，前端可见
- [ ] retry 路径与现有 Policy Matrix retry 计数合流，不重复计数

## Technical Details

- shell hook：`subprocess.run(cmd, timeout=...)`，stdout/stderr 截断到 ~4KB 传回
- webhook：`httpx.post(url, json=ctx, timeout=30s)`，HTTP 2xx = pass
- builtin：从 §004 registry 查找对应实现

## Dependencies

- 002 完成

## Effort Estimate

- Size: M
- Hours: 16-24
- Parallel: false

## Definition of Done

- [ ] 集成测试：用 mock hook 验证 pass/fail 两条路径
- [ ] e2e 手测：turn 失败 → 看到 retry → 改 hook → 通过
