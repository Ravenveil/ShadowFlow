---
name: UI 配置面板 + e2e 浏览器验证
status: open
created: 2026-05-26T14:20:03Z
updated: 2026-05-26T14:20:03Z
depends_on: [002, 004]
parallel: true
conflicts_with: []
priority: P1
size: M
hours: 16-24
---

# Task: UI 配置面板 + e2e 浏览器验证

## Description

在 team 设置页加 "校验脚本" tab，让用户可视化挂载 / 删除 / 测试运行 validation hook。
完成后做一次完整 e2e 浏览器手测。

## Acceptance Criteria

- [ ] `src/components/team-settings/ValidationHooksTab.tsx` 新建
- [ ] 功能：列出当前 hooks / 添加（select kind 从 builtin registry / 自填 shell / webhook）/ 删除 / 启停 / 测试运行
- [ ] 测试运行：mock 一个空 turn_ctx，立即返回 pass/fail 给用户
- [ ] **浏览器手测 e2e 通过**（沿用 `feedback_frontend_dod_browser_verify` memory）：
  - 起 dev server 8002 + Vite
  - 浏览器进 team 设置 → 加 tsc-check hook → 故意改坏代码 → 起新 turn → 看到 retry → 修代码 → 通过
  - console 零 error
  - 截图 3 张归档 `_evidence/s-harness-scripts-005/`

## Technical Details

- 复用现有 team-settings 路由 + Policy Matrix Panel 同构样式
- 用 lucide-react 单色线性图标（不用 emoji，沿用 `feedback_no_system_emoji_icons` memory）

## Dependencies

- 002 完成（API 可用）
- 004 完成（registry 可查）
- 与 003 可并行（UI 不依赖 runtime 执行）

## Effort Estimate

- Size: M
- Hours: 16-24
- Parallel: true

## Definition of Done

- [ ] tsc 干净 + vite build 通过
- [ ] 浏览器 e2e 全过 + 截图归档
- [ ] commit + push
