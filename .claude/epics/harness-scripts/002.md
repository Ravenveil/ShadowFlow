---
name: Hook 数据模型 + Teams API 扩展
status: open
created: 2026-05-26T14:20:03Z
updated: 2026-05-26T14:20:03Z
depends_on: [001]
parallel: false
conflicts_with: []
priority: P0
size: S
hours: 8-12
---

# Task: Hook 数据模型 + Teams API 扩展

## Description

按 001 设计文档落地 `team.validation_hooks` 字段 + 对应 CRUD endpoints。

## Acceptance Criteria

- [ ] `shadowflow/runtime/contracts_builder.py` 或 teams record schema 加 `validation_hooks: List[ValidationHookSpec]`
- [ ] `ValidationHookSpec` Pydantic 模型：`{id, kind: 'shell'|'webhook'|'builtin', config, on_fail: 'retry'|'blocker', enabled}`
- [ ] `shadowflow/api/teams.py` 加 endpoints：
  - `GET /api/teams/{team_id}/validation-hooks`
  - `PUT /api/teams/{team_id}/validation-hooks`
- [ ] 写入持久化到 team JSON 文件（与现有 workflow / policy_matrix 同样存储）
- [ ] 单元测试覆盖 CRUD + schema 校验

## Technical Details

- 复用 `shadowflow/api/teams.py:247 TeamWorkflow` 同构模式
- 凭证字段（如 webhook URL token）走 secrets ref 不内联

## Dependencies

- 001 完成

## Effort Estimate

- Size: S
- Hours: 8-12
- Parallel: false（blocks 003）

## Definition of Done

- [ ] 单测全绿
- [ ] curl 验证 GET/PUT endpoints
- [ ] 文档更新（API reference）
