---
name: Skill "deterministic step" 类型设计 + schema 扩展
status: open
created: 2026-05-26T14:20:03Z
updated: 2026-05-26T14:20:03Z
depends_on: []
parallel: false
conflicts_with: []
priority: P0
size: M
hours: 16-24
---

# Task: Skill "deterministic step" 类型设计 + schema 扩展

## Description

文章 §1.1 主张 Skill 是"执行剧本"——固定步骤、不容现场发挥。我们当前 skill 是 prompt 模板，
确定性靠 agent 听话。本 task 给 skill schema 加 "deterministic step" 类型，可触发本地命令 /
HTTP / 内置工具调用，把"剧本"从虚的变成实的。

## Acceptance Criteria

- [ ] 现有 skill SKILL.md frontmatter schema 扩展：支持 `steps: List[Step]` 字段
- [ ] `Step` 类型枚举：`{kind: 'llm'|'shell'|'http'|'builtin_tool', config, on_fail}`
- [ ] skill-ingest pipeline 解析 + 校验 steps 字段
- [ ] 执行引擎 `shadowflow/runtime/skill_step_executor/` 按 step 顺序执行
- [ ] 向后兼容：没填 steps 的 skill 仍按现有 prompt 模板模式跑

## Technical Details

- shell/http step 必须在沙箱里跑（超时 + 资源限制）
- builtin_tool step 复用 [[harness-scripts]] 004 validator registry 同源思路

## Dependencies

- 无前置

## Effort Estimate

- Size: M
- Hours: 16-24
- Parallel: false（blocks 003）

## Definition of Done

- [ ] schema + 执行引擎单测全绿
- [ ] 一个示例 skill 含 shell step 跑通
