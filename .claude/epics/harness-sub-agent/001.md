---
name: agent-team-blueprint 生成器深化
status: open
created: 2026-05-26T14:20:03Z
updated: 2026-05-26T14:20:03Z
depends_on: []
parallel: true
conflicts_with: []
priority: P0
size: M
hours: 16-20
---

# Task: agent-team-blueprint 生成器深化

## Description

让"自然语言 goal → team blueprint"主路径（StartPage composer + `agent-team-blueprint` 模板）
生成的 `AgentBlueprint` 把 RoleProfile 深度契约字段也填进，而不是只填 name/soul/role。

## Acceptance Criteria

- [ ] 找到 `agent-team-blueprint` 模板（candidates: `server/.shadowflow/skills/` 或后端
      `agent_team_blueprint` 配置）
- [ ] 在生成 prompt 里强制要求 LLM 为每个角色填：
  - `responsibilities: List[str]`（至少 2 条）
  - `constraints: List[str]`（至少 1 条）
  - `handoff_rules: List[HandoffRule]`（至少 1 条，描述向谁交接 / 何时升级）
- [ ] 添加 schema 校验：缺字段则 retry generator 一次
- [ ] 用 3 个不同 goal 跑 e2e（"做加密快照工具" / "写一份市场调研" / "审查这段代码"），确认深度字段被填充

## Technical Details

- 参考 `shadowflow/runtime/builder_service.py:64 generate_blueprint` 接口
- prompt 工程模式：JSON schema in context + few-shot example

## Dependencies

- 无前置（与 002/003/004 可并行）

## Effort Estimate

- Size: M
- Hours: 16-20
- Parallel: true

## Definition of Done

- [ ] 3 个 e2e goal 验证通过，生成的 blueprint 含深度字段
- [ ] retry 校验机制覆盖
